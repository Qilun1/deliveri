# DeliVeri Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# SUPABASE (Required)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# ===========================================
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ===========================================
# CLERK (Required)
# Get this from: https://dashboard.clerk.com -> API Keys
# ===========================================
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# ===========================================
# IMPORTANT SECURITY NOTES
# ===========================================
# 1. NEVER add secret keys to this file (sk_*, service_role keys, etc.)
# 2. The VITE_ prefix exposes these values to the browser - only use for public keys
# 3. Server-side secrets go in Supabase Edge Function secrets:
#    npx supabase secrets set CLERK_SECRET_KEY=sk_live_...
#    npx supabase secrets set GEMINI_API_KEY=your_key
#    npx supabase secrets set ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
#
# ===========================================
# EDGE FUNCTION SECURITY
# ===========================================
# ALLOWED_ORIGINS - Comma-separated list of allowed CORS origins for edge functions
# Set this in Supabase secrets to restrict which domains can call your edge functions:
#    npx supabase secrets set ALLOWED_ORIGINS=https://yourdomain.com
# Use '*' for development only (not recommended for production)
